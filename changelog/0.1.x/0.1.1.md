---
summary: "Metadata polish — action-first package and plugin descriptions, npm-scoped README header, and a repository field on the MCPB manifest."
breaking: false
security: false
---

# 0.1.1 — 2026-06-14

## Added

- **`manifest.json` `repository`** — added the git repository object to the MCPB manifest.

## Changed

- **Descriptions** — rewrote `package.json`, `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, and the Docker OCI image label to lead with the action (decode N-numbers, search the registry) rather than the format.
- **README `<h1>`** — now the scoped package name `@cyanheads/faa-aircraft-registry-mcp-server`, matching the published npm identity.
- **Dependencies** — `better-sqlite3` ^12.10.0 → ^12.10.1.
